<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jenia.myengemprove">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:theme="@style/SplashTheme">

            <!-- android:theme="@style/AppTheme.NoActionBar"> -->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".ActivityDictionary" />
        <activity android:name=".Read" />
        <activity android:name=".ChangeBase" />
        <activity
            android:name=".DictionaryView"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".MyTestingProcess" />
        <activity android:name=".Domashka" />
        <activity android:name=".Translater"></activity>
    </application>

</manifest>